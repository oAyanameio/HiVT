# 可靠性实验结果汇总

数据来源：`/home/lbh/HiVT/runs/hivt_reliability/` 下保留的 TensorBoard event 文件。

## 指标含义与统计口径

### 1. 数据集与时间窗口

- 数据集：`ArgoverseV1Dataset` 验证集
- 历史帧数：`20`
- 未来帧数：`30`
- 采样频率：`10 Hz`
- 历史时长：`2.0 秒`
- 预测时长：`3.0 秒`

当前代码里，每个样本总共使用 `50` 个时间戳：

- 历史部分：`timestamps[:20]`
- 未来部分：`timestamps[20:]`

对应代码：

- [argoverse_v1_dataset.py](/home/lbh/HiVT/datasets/argoverse_v1_dataset.py:167)
- [argoverse_v1_dataset.py](/home/lbh/HiVT/datasets/argoverse_v1_dataset.py:229)

### 2. 这些 `val_*` 指标是不是在整个验证集上算的？

是。

这些 `val_*` 指标都不是单个 batch 的数，而是在整个 `val_dataloader()` 上逐 batch 累积，最后在一个 validation epoch 结束时输出。

对应代码：

- [argoverse_v1_datamodule.py](/home/lbh/HiVT/datamodules/argoverse_v1_datamodule.py:60)
- [hivt.py](/home/lbh/HiVT/models/hivt.py:338)

### 3. 预测精度指标

#### `val_minADE`

`val_minADE` 不是在所有 actor 上算的，而是**只在每个场景的目标车 `AGENT` 上算**。

当前代码流程是：

1. 用 `data['agent_index']` 取出每个场景的目标车
2. 模型输出 `K=6` 条预测轨迹
3. 先按终点误差最小选出 best mode
4. 再用这条 best mode 计算整条未来轨迹的 ADE
5. 最后在整个验证集所有场景上取平均

当前实现对应的含义是：

```text
best mode = argmin_k FDE_k
ADE = mean_t ||y_pred(t) - y_gt(t)||_2
val_minADE = 验证集所有场景目标车的平均值
```

对应代码：

- [hivt.py](/home/lbh/HiVT/models/hivt.py:340)
- [ade.py](/home/lbh/HiVT/metrics/ade.py:32)

#### `val_minFDE`

同样只在目标车上计算。

流程是先选出终点误差最小的 best mode，再计算这条轨迹最后一个未来时刻的终点误差：

```text
best mode = argmin_k FDE_k
FDE = ||y_pred(T) - y_gt(T)||_2
val_minFDE = 验证集所有场景目标车的平均值
```

对应代码：

- [hivt.py](/home/lbh/HiVT/models/hivt.py:343)
- [fde.py](/home/lbh/HiVT/metrics/fde.py:32)

#### `val_minMR`

同样只在目标车上计算。

当前实现里 miss 的定义是：

```text
MR = mean( 1(FDE > 2.0 m) )
```

也就是 best mode 的终点误差如果大于 `2.0m`，这个样本就记为 miss。

对应代码：

- [hivt.py](/home/lbh/HiVT/models/hivt.py:344)
- [mr.py](/home/lbh/HiVT/metrics/mr.py:16)

### 4. 可靠性指标

#### `val_mode_AUROC`

这是 **mode 级失败检测** 的 ROC-AUC。

- 样本单位：一个 `(actor, mode)` 对
- 样本范围：所有有效 actor，经 `valid_mask` 过滤
- 分数：预测的 `mode_risk`
- 标签：`mode_targets`

直观上，它表示：

> 随机抽一个正样本 mode 和一个负样本 mode，模型给正样本更高风险分数的概率有多大。

对应代码：

- [hivt.py](/home/lbh/HiVT/models/hivt.py:407)
- [reliability_metrics.py](/home/lbh/HiVT/metrics/reliability_metrics.py:120)

#### `val_mode_AUPRC`

这是同一个 mode 级失败检测任务上的 PR-AUC。

- 样本单位：一个 `(actor, mode)` 对
- 样本范围：所有有效 actor，经 `valid_mask` 过滤

当正样本比例不均衡时，AUPRC 往往比 AUROC 更敏感。

对应代码：

- [hivt.py](/home/lbh/HiVT/models/hivt.py:408)
- [reliability_metrics.py](/home/lbh/HiVT/metrics/reliability_metrics.py:128)

#### `val_mode_BrierScore`

这是预测风险概率与二值失败标签之间的均方误差：

```text
Brier = mean( (risk - target)^2 )
```

- 样本单位：一个 `(actor, mode)` 对
- 样本范围：所有有效 actor，经 `valid_mask` 过滤

越低越好。

对应代码：

- [hivt.py](/home/lbh/HiVT/models/hivt.py:409)
- [reliability_metrics.py](/home/lbh/HiVT/metrics/reliability_metrics.py:19)

#### `val_mode_ECE`

这是 mode 级风险概率的校准误差。

- 样本单位：一个 `(actor, mode)` 对
- 样本范围：所有有效 actor，经 `valid_mask` 过滤
- 当前实现：`10` 个 bin

当前代码里的定义是：

```text
ECE = Σ_b (n_b / N) * |avg_conf_b - avg_target_b|
```

越低越好。

对应代码：

- [hivt.py](/home/lbh/HiVT/models/hivt.py:410)
- [reliability_metrics.py](/home/lbh/HiVT/metrics/reliability_metrics.py:42)

### 5. 风险标签命中率

#### `val_mode_risk_target_rate`

这是主 `mode_targets` 的正样本率，统计范围是所有有效 `(actor, mode)` 对。

在当前保留的实验设置里：

```text
mode_target_policy = fde_only
```

所以它本质上就是：

```text
1(FDE_k > risk_fde_threshold)
```

这个标签在所有有效 `(actor, mode)` 上的平均值。

对应代码：

- [reliability.py](/home/lbh/HiVT/models/reliability.py:387)
- [reliability.py](/home/lbh/HiVT/models/reliability.py:491)

#### `val_fde_risk_target_rate`

这是原始 FDE 风险标签的正样本率，统计范围也是所有有效 `(actor, mode)` 对。

当前阈值：

- `risk_fde_threshold = 2.0`

对应代码：

- [reliability.py](/home/lbh/HiVT/models/reliability.py:54)
- [reliability.py](/home/lbh/HiVT/models/reliability.py:491)

#### `val_ade_risk_target_rate`

这是原始 ADE 风险标签的正样本率：

```text
1(ADE_k > ade_threshold)
```

统计范围仍然是所有有效 `(actor, mode)` 对。

对应代码：

- [reliability.py](/home/lbh/HiVT/models/reliability.py:72)
- [reliability.py](/home/lbh/HiVT/models/reliability.py:491)

#### `val_miss_risk_target_rate`

这是 miss 风险标签的正样本率：

```text
1(FDE_k > miss_threshold)
```

在保留的 stage-1 实验里：

- `risk_miss_threshold = 4.0`

对应代码：

- [reliability.py](/home/lbh/HiVT/models/reliability.py:88)
- [reliability.py](/home/lbh/HiVT/models/reliability.py:491)

#### `val_conflict_risk_target_rate`

这是 conflict 标签的正样本率，统计范围是所有有效 `(actor, mode)` 对。

在保留实验里，conflict 的定义是：

- `risk_conflict_scope = target_to_neighbors`
- 用目标车预测未来和邻车 GT 未来比较
- 距离低于阈值
- 至少连续 `risk_conflict_min_frames = 2` 帧接近

它是一个 mode 级安全代理指标，不是真实碰撞标注。

这里有一个很重要的口径限制：

- `risk_conflict_scope = target_to_neighbors` 只会给目标车 mode 打 conflict 标签；
- 但 `val_conflict_risk_target_rate` 是在所有有效 `(actor, mode)` 对上取均值；
- 所以这个指标会被大量非目标 actor 稀释。

因此它更适合回答“conflict 会不会污染全局主监督”，不适合单独判断目标车 conflict 标签是否可训练。

对应代码：

- [reliability.py](/home/lbh/HiVT/models/reliability.py:219)
- [reliability.py](/home/lbh/HiVT/models/reliability.py:491)

#### `val_offroad_risk_target_rate`

这是 off-road 代理标签的正样本率，统计范围也是所有有效 `(actor, mode)` 对。

当前实现并不是真实 drivable area polygon 越界，而是基于 lane support 的距离代理，因此只能当辅助安全代理，不能当严格越界真值。

对应代码：

- [reliability.py](/home/lbh/HiVT/models/reliability.py:319)
- [reliability.py](/home/lbh/HiVT/models/reliability.py:491)

#### `val_scene_risk_target_rate`

这是 scene 级风险标签的正样本率，统计单位是 scene。

当前保留 run 使用的是：

```text
scene_target_policy = target_mode_rate
risk_scene_rate_threshold = 0.67
```

也就是：

```text
scene_target = 1(target actor 的高风险 mode 比例 > 0.67)
```

对应代码：

- [reliability.py](/home/lbh/HiVT/models/reliability.py:145)
- [reliability.py](/home/lbh/HiVT/models/reliability.py:491)

### 6. 预测分数均值

#### `val_mode_risk_pred_mean`

这是模型输出的 `mode_risk` 概率均值，统计范围是所有 `(actor, mode)` 对。

它不是性能指标，但有助于检查是否出现明显塌缩，比如全接近 `1.0` 或全接近 `0.0`。

#### `val_scene_risk_pred_mean`

这是模型输出的 `scene_risk` 概率均值，统计范围是所有 scene。

同样主要用于检查分布是否异常。

## 当前保留实验结果

### 0. 宿主原始 `pi` 与 freeze-backbone 诊断补充（2026-06-28）

这一轮补的是之前缺失的三类诊断：

- 宿主原始 `pi` 的 naive reliability baseline
- `mode_risk` 与实际 `FDE` 的排序相关性
- `scene` 级校准指标

同时补做了 freeze-backbone 与 reranking-only 对照。

#### 0.1 宿主 checkpoint 的 naive baseline

使用宿主 `HiVT-64` checkpoint：

```text
/home/lbh/HiVT/checkpoints/HiVT-64/checkpoints/epoch=63-step=411903.ckpt
```

以

```text
risk_proxy_naive = -log softmax(pi_k)
```

作为 mode-level failure risk 代理，对 `FDE > 2.0m` 的标签计算得到：

```text
naive_mode_AUROC = 0.700041
naive_mode_AUPRC = 0.714026
```

这说明宿主原始 `pi` 本身就有一定 failure detection 能力，但明显低于之前 plugin run 中 `mode_risk` 的 `AUROC/AUPRC`。

#### 0.2 Freeze-backbone 结果

正确的 freeze-backbone 设置是：

- 从宿主 `HiVT-64` checkpoint 热启动 backbone
- `freeze_backbone=true`
- 只训练 `reliability_module`

注意：如果不先加载宿主 checkpoint，而是直接冻结随机初始化 backbone，那么预测指标会失真，那种结果不能用于结论。

本轮正确 freeze-backbone run 的验证指标为：

```text
val_minADE = 0.574
val_minFDE = 0.803
val_minMR  = 0.0508
```

与宿主 baseline 基本一致。可用结论是：

1. freeze-backbone 成功避免了联合训练对宿主预测性能的破坏；
2. 之前 `minADE/minFDE/MR` 的退化，主要来自 reliability loss 对 backbone 的梯度干扰，而不是 reliability 分支本身一定无效。

#### 0.3 Freeze-backbone checkpoint 上的风险诊断

对 freeze-backbone 的正确 checkpoint 继续做分析，得到：

```text
naive_mode_AUROC        = 0.695946
naive_mode_AUPRC        = 0.713348
mode_risk_fde_spearman  = 0.748202
scene_BrierScore        = 0.254131
scene_ECE               = 0.235648
```

这里的含义需要分开看：

1. `naive_mode_AUROC/AUPRC` 仍然只是在评估宿主原始 `pi` 的 failure detection 能力；
2. `mode_risk_fde_spearman=0.7482` 说明 `mode_risk` 对真实 `FDE` 已经具备较强排序能力；
3. `scene_BrierScore/ECE` 仍然偏差较大，说明 scene head 目前更像“有粗判别性但未校准”的分支。

进一步按 follow-up plan 补做了 scene calibration 分桶统计。对同一个 freeze-backbone checkpoint，在 `32` 个 val batch 上得到：

```text
scene_count                  = 256
scene_BrierScore             = 0.254131
scene_ECE                    = 0.235648
scene_pred_mean              = 0.536430
scene_target_rate            = 0.300781
scene_precision@0.5          = 0.407407
scene_recall@0.5             = 0.714286
scene_specificity@0.5        = 0.553073
scene_accuracy@0.5           = 0.601562
```

按概率分桶后的 calibration 结果如下：

| prob bin | count | avg_prob | avg_target |
|---|---:|---:|---:|
| `0.1-0.2` | 11 | 0.166 | 0.091 |
| `0.2-0.3` | 32 | 0.250 | 0.156 |
| `0.3-0.4` | 32 | 0.352 | 0.063 |
| `0.4-0.5` | 46 | 0.448 | 0.304 |
| `0.5-0.6` | 40 | 0.549 | 0.325 |
| `0.6-0.7` | 27 | 0.643 | 0.407 |
| `0.7-0.8` | 25 | 0.747 | 0.360 |
| `0.8-0.9` | 27 | 0.836 | 0.370 |
| `0.9-1.0` | 16 | 0.941 | 0.750 |

这里暴露出的模式非常明确：

1. `scene_pred_mean = 0.536` 明显高于 `scene_target_rate = 0.301`，整体存在系统性正偏；
2. 几乎所有 `0.5+` 的高概率 bin 都是过置信的，尤其 `0.7-0.9` 这几档，预测概率约 `0.75~0.84`，真实命中率却只有 `0.36~0.37`；
3. 即使在最高的 `0.9-1.0` bin，真实正样本率也只有 `0.75`，仍然说明当前 scene 概率不能按字面概率解释；
4. 用 `0.5` 做简单决策阈值时，scene head 的 `recall=0.714` 还可以，但 `precision=0.407`、`specificity=0.553` 都偏低，说明它更像“偏激进的粗筛器”，而不是可直接部署的 calibrated probability。

进一步按 follow-up plan 补做了 backbone 梯度干扰分析。由于当前 joint run 目录没有保留 checkpoint，这一步改用 freeze-backbone 的 reliability checkpoint 做反事实诊断：临时打开 backbone 梯度，分别计算 `pred_loss`、加权后的 `mode reliability loss`、加权后的 `scene loss`，比较它们在 `local_encoder / global_interactor / decoder` 上的梯度夹角与范数比例。

在 `8` 个 val batch 上得到：

```text
local_encoder:
  mode_vs_pred:        mean_cosine = -0.1555, negative_fraction = 0.875, norm_ratio = 0.2257
  scene_vs_pred:       mean_cosine =  0.1967, negative_fraction = 0.375, norm_ratio = 0.2183
  reliability_vs_pred: mean_cosine =  0.0909, negative_fraction = 0.500, norm_ratio = 0.2854

global_interactor:
  mode_vs_pred:        mean_cosine =  0.0151, negative_fraction = 0.500, norm_ratio = 0.4095
  scene_vs_pred:       mean_cosine = -0.0916, negative_fraction = 0.750, norm_ratio = 0.2269
  reliability_vs_pred: mean_cosine = -0.0473, negative_fraction = 0.625, norm_ratio = 0.4581

decoder:
  mode_vs_pred:        mean_cosine =  0.0214, negative_fraction = 0.250, norm_ratio = 1.7325
  scene_vs_pred:       mean_cosine =  0.0276, negative_fraction = 0.250, norm_ratio = 0.0022
  reliability_vs_pred: mean_cosine =  0.0215, negative_fraction = 0.250, norm_ratio = 1.7323
```

这一步把之前“backbone 受 reliability loss 干扰”的说法从日志级弱结论推进成了结构级证据：

1. `mode reliability loss` 对 `local_encoder` 的梯度与 `pred_loss` 明显冲突，`87.5%` 的 batch 为负夹角；
2. `scene loss` 的主要干扰位置更偏 `global_interactor`，负夹角比例达到 `75%`；
3. 合并后的 `reliability_total` 对 `global_interactor` 仍呈轻度负相关，说明联合训练时 backbone 的确存在系统性拉扯；
4. `decoder` 上 reliability 梯度幅度大，但平均夹角接近 `0`，更像是改变输出层更新强度，而不是稳定朝与 `pred_loss` 相反方向推进。

#### 0.4 Reranking-only 结果

在同一个 freeze-backbone checkpoint 上，对比 `original pi` 与 `pi - alpha * risk` 的 top-1 重排结果：

| alpha | original minADE | original minFDE | original MR | reranked minADE | reranked minFDE | reranked MR | top1 change rate |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0.2 | 1.198633 | 2.575924 | 0.425781 | 1.201279 | 2.585675 | 0.437500 | 0.023438 |
| 0.5 | 1.198633 | 2.575924 | 0.425781 | 1.209395 | 2.598438 | 0.445312 | 0.074219 |
| 1.0 | 1.198633 | 2.575923 | 0.425781 | 1.219001 | 2.613258 | 0.449219 | 0.128906 |

可用结论：

1. reranking 确实改变了 top-1 mode，不是“risk 分数完全没起作用”；
2. 当前 `pi - alpha * risk` 公式在 `alpha=0.2/0.5/1.0` 下都会带来小幅退化；
3. 因此当前问题不是 `mode_risk` 没学到，而是“当前重排映射不合适”。

进一步按评审意见补做了概率空间 reranking 对照。当前脚本新增了两类公式：

- `prob_product`: `log_softmax(pi) + alpha * log(1 - risk + eps)`
- `logprob_minus_norm_risk`: `log_softmax(pi) - alpha * normalized_risk`

并支持只在 `top-k` 候选 mode 内重排。

同一 freeze-backbone checkpoint、同一 32 个验证 batch 上的结果如下：

| method | alpha | top-k | original minADE | original minFDE | original MR | reranked minADE | reranked minFDE | reranked MR | top1 change rate |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `prob_product` | 0.5 | 3 | 1.198633 | 2.575924 | 0.425781 | 1.172185 | 2.487767 | 0.449219 | 0.214844 |
| `prob_product` | 1.0 | 3 | 1.198633 | 2.575923 | 0.425781 | 1.172185 | 2.487766 | 0.449219 | 0.214844 |
| `logprob_minus_norm_risk` | 0.5 | 3 | 1.198633 | 2.575923 | 0.425781 | 1.207332 | 2.574770 | 0.472656 | 0.324219 |

当前可用结论：

1. 把重排切回概率空间后，`minADE/minFDE` 已不再像旧公式那样稳定退化；
2. 但 `MR` 仍然明显变差，说明“概率语义正确”还不等于“mode selection 已经可直接获益”；
3. 在当前脚本口径下，`prob_product` 的最优重排实际已经落在原始 top-3 内；`alpha=0.5` 与 `1.0` 给出相同 argmax，说明当前更敏感的不是这两个 alpha，而是公式本身；
4. `logprob_minus_norm_risk` 明显更差，`top1 change rate=0.324219`，`MR=0.472656`，说明单纯把标准化 risk 线性扣到 log-prob 上会引入更多坏翻转。

进一步做 case analysis 后，`prob_product + alpha=1.0 + top_k=3` 的 `256` 个验证样本分桶如下：

```text
case_mean_fde_delta          = -0.088157
case_hit_to_miss_count       = 9
case_miss_to_hit_count       = 3
case_still_miss_improved     = 16
case_still_miss_worsened     = 10
case_still_miss_unchanged    = 80
case_still_hit_improved      = 6
case_still_hit_worsened      = 11
case_still_hit_unchanged     = 121
```

这组分桶给出的核心解释是：

1. `MR` 变差的直接原因不是“大量样本被彻底换坏”，而是 `hit->miss = 9` 多于 `miss->hit = 3`，净增 `6` 个 miss；
2. `minFDE` 下降主要来自 `still_miss_improved = 16`，也就是一批“本来就 miss 的样本”被拉近了，但还没有跨过 `2.0m` 的 MR 阈值；
3. 因而当前 reranking 更像是在优化连续的终点误差，而不是稳定优化 `miss` 这个阈值事件；
4. 这也说明后续若还要追求 `MR` 收益，目标不能只继续对齐 `FDE` 排序，还要单独分析阈值附近样本。

进一步按 follow-up plan 补做了 conflict threshold audit。使用宿主 checkpoint：

```text
/home/lbh/HiVT/checkpoints/HiVT-64/checkpoints/epoch=63-step=411903.ckpt
```

在 `32` 个 val batch 上，对 `risk_conflict_threshold = 1.0 / 1.5 / 2.0 / 3.0` 扫描的结果如下：

| tau_col | conflict_risk_target_rate | target_actor_conflict_rate | mode_risk_target_rate |
|---|---:|---:|---:|
| `1.0` | `0.000752` | `0.012370` | `0.435987` |
| `1.5` | `0.001206` | `0.019531` | `0.435987` |
| `2.0` | `0.001794` | `0.027995` | `0.435987` |
| `3.0` | `0.009042` | `0.130859` | `0.435987` |

同批数据上的 `min_pair_dist` 分位数为：

```text
q10 = 2.8559 m
q50 = 6.3546 m
q90 = 27.9861 m
```

这组结果说明：

1. 如果继续按所有有效 `(actor, mode)` 对统计，那么即使 `tau_col = 3.0m`，`conflict_risk_target_rate` 也只有 `0.904%`，仍未进入 `5%~15%` 的常规可训练区间；
2. 但如果只看真正会被 `target_to_neighbors` 打标的目标车 mode，`target_actor_conflict_rate` 在 `tau_col = 3.0m` 时已经达到 `13.09%`，进入了可训练区间；
3. 因此当前更根本的问题不是“conflict 标签完全死掉”，而是“target-only 标签定义”和“all-actor 汇总统计口径”不一致，导致之前的 `conflict_risk_target_rate ≈ 0.0007` 被严重稀释；
4. 这也意味着后续如果要继续保留 conflict 相关论据，应同时报告：
   - `conflict_risk_target_rate`：监控是否污染主监督
   - `target_actor_conflict_rate`：判断 target-only conflict 是否可训练

### 1. `stage1_fix_budget64_fdeonly_targetmode067`

配置：

- `mode_target_policy = fde_only`
- `scene_target_policy = target_mode_rate`
- `risk_scene_rate_threshold = 0.67`

结果：

```text
val_minADE                   = 0.622291
val_minFDE                   = 0.950928
val_minMR                    = 0.0703125
val_mode_AUROC               = 0.852163
val_mode_AUPRC               = 0.796411
val_mode_BrierScore          = 0.159168
val_mode_ECE                 = 0.0691175
val_risk_loss                = 0.478258
val_scene_loss               = 0.646788
val_mode_risk_target_rate    = 0.45711
val_scene_risk_target_rate   = 0.335938
val_mode_risk_pred_mean      = 0.531409
val_scene_risk_pred_mean     = 0.415996
val_fde_risk_target_rate     = 0.45711
val_ade_risk_target_rate     = 0.456845
val_miss_risk_target_rate    = 0.261163
val_conflict_risk_target_rate= 0.000723963
val_offroad_risk_target_rate = 0.230842
```

### 2. `stage1_fix_budget128_fdeonly_targetmode067`

配置：

- `mode_target_policy = fde_only`
- `scene_target_policy = target_mode_rate`
- `risk_scene_rate_threshold = 0.67`

结果：

```text
val_minADE                   = 0.610951
val_minFDE                   = 0.866362
val_minMR                    = 0.0703125
val_mode_AUROC               = 0.873523
val_mode_AUPRC               = 0.835783
val_mode_BrierScore          = 0.144478
val_mode_ECE                 = 0.0301806
val_risk_loss                = 0.43723
val_scene_loss               = 0.788139
val_mode_risk_target_rate    = 0.459108
val_scene_risk_target_rate   = 0.394531
val_mode_risk_pred_mean      = 0.473831
val_scene_risk_pred_mean     = 0.634967
val_fde_risk_target_rate     = 0.459108
val_ade_risk_target_rate     = 0.456631
val_miss_risk_target_rate    = 0.264216
val_conflict_risk_target_rate= 0.000677719
val_offroad_risk_target_rate = 0.223118
```

## 当前总结

### 已解决的问题

1. 标签与统计口径的基础问题已经定位清楚：
   - `mode_risk_target_rate` 与 `scene_risk_target_rate` 不再塌到 `1.0`
   - conflict 的极低全局正样本率已确认主要来自 target-only 定义与 all-actor 汇总不一致

2. 基础对照已经补齐：
   - 宿主原始 `pi` 的 naive baseline 已补齐
   - freeze-backbone 对照已补齐
   - reranking-only 对照已补齐

3. 联合训练退化来源已经从日志级归因推进到结构级证据：
   - 不再只是“joint 比 freeze 差”
   - 已进一步确认 backbone 编码层确实受到 reliability loss 干扰

### 已产生的结果

1. 当前 mode 级风险预测已经具备可判别性：
   - `AUROC` 达到 `0.85 ~ 0.87`
   - `AUPRC` 达到 `0.80 ~ 0.84`
   - `ECE` 降到 `0.03 ~ 0.07`

2. 宿主原始 `pi` 的 naive baseline 明显低于 reliability 分支：
   - naive `AUROC/AUPRC` 约为 `0.70/0.71`
   - plugin `mode_risk` 在已有 run 中达到 `0.87/0.84`

3. freeze-backbone 已经验证通过：
   - 它可以基本保住宿主 `minADE / minFDE / MR`
   - `mode_risk` 与真实 `FDE` 的排序相关性较强，`Spearman = 0.7482`

4. 概率空间 reranking 已经带来部分收益：
   - 当前 `prob_product` 公式可以改善 `minADE / minFDE`

5. 联合训练退化的结构级证据已经拿到：
   - `mode loss` 主要干扰 `local_encoder`
   - `scene loss` 更偏干扰 `global_interactor`

### 新问题

1. reranking 还没有转化成 `MR` 收益：
   - 当前改善集中在连续误差 `minADE / minFDE`
   - `MR` 仍未改善

2. scene head 仍然明显失配：
   - `scene_ECE ≈ 0.236`
   - 高概率 bin 存在系统性过置信

3. conflict 分支如果继续保留，后续口径必须切换：
   - 全 actor 口径下仍然太稀
   - target-only 口径下 `tau_col = 3.0m` 才进入可训练区间

4. 当前绝对数值还不能直接当论文最终结果：
   - 小预算下趋势明确
   - 但仍待中等预算验证稳定性

## 2026-06-29：near-threshold objective 链路修正与 smoke 结果

这一轮做的不是扩大训练，而是把 freeze-backbone 下的目标函数实验链路先修通，并验证新 objective 至少能稳定进入 checkpoint 和 reranking 对照。

### 1. 本轮实现内容

- 新增 `FDE=2.0m` 阈值附近样本加权 BCE：
  - `mode_risk_threshold_weight_enabled`
  - `mode_risk_threshold_weight_radius`
  - `mode_risk_threshold_weight_peak`
  - `mode_risk_threshold_weight_base`
- 新增 `top-k + near-threshold` pairwise rank filtering：
  - `mode_risk_rank_top_k`
  - `mode_risk_rank_near_threshold_only`
  - `mode_risk_rank_threshold_radius`
- `rank loss` 的连续监督从 `ADE` 切到 `FDE`，更贴近当前 `MR` / reranking 目标。

同时修掉了两个会污染实验解释的链路问题：

1. `argparse type=bool` 会把 shell 里的 `"false"` 解析成 `True`；
2. 多个分析脚本会用 CLI 默认值覆盖 checkpoint `hyper_parameters`，导致 reliability checkpoint 被错误加载成 `use_reliability=False`。

### 2. 单测状态

```text
pytest tests/test_reliability.py \
       tests/test_reliability_losses.py \
       tests/test_reliability_presets.py \
       tests/test_reliability_analysis.py \
       tests/test_utils.py -q

40 passed
```

### 3. smoke run 配置

run:

```text
freeze_backbone_threshold_objective_smoke
```

关键配置：

```text
freeze_backbone                    = true
rank_loss_weight                   = 0.05
mode_risk_threshold_weight_enabled = true
mode_risk_threshold_weight_radius  = 0.25
mode_risk_threshold_weight_peak    = 3.0
mode_risk_rank_top_k               = 3
mode_risk_rank_near_threshold_only = true
mode_risk_rank_threshold_radius    = 0.2
scene_target_policy                = target_best_mode_fail
```

### 4. 同口径 reranking 对照

口径保持为：

```text
prob_product + rerank_alpha=1.0 + rerank_top_k=3 + 32 val batches
```

旧 freeze-backbone checkpoint：

```text
original_minADE = 1.198633
original_minFDE = 2.575924
original_minMR  = 0.425781
reranked_minADE = 1.172185
reranked_minFDE = 2.487766
reranked_minMR  = 0.449219
rerank_top1_change_rate = 0.214844
miss_to_hit_count       = 3
hit_to_miss_count       = 9
```

新 smoke checkpoint：

```text
original_minADE = 1.198633
original_minFDE = 2.575924
original_minMR  = 0.425781
reranked_minADE = 1.184561
reranked_minFDE = 2.530490
reranked_minMR  = 0.457031
rerank_top1_change_rate = 0.222656
miss_to_hit_count       = 2
hit_to_miss_count       = 10
```

### 5. 当前可用结论

1. 新 objective 的训练/评估链路已经打通；
2. 参数确实写入了 checkpoint，不再只是 shell 层假配置；
3. 但这轮 `1 epoch / 2 train batches` 的 smoke 结果没有改善 `MR`；
4. 相比旧 freeze-backbone checkpoint，它在同样 reranking 口径下的 `MR` 还更差；
5. 因此这套 near-threshold objective 目前只能算“已实现、可继续调”，还不能算有效方案。

## 2026-06-30：reranking rule 反事实扫描与 case 复盘

这一轮不再继续先开新训练，而是直接验证：

```text
当前 MR 退化，到底来自 risk head 本身，还是来自 reranking rule 太激进？
```

### 1. 本轮新增脚本与单测

新增脚本：

- [eval_reranking.py](/home/lbh/HiVT/scripts/eval_reranking.py)
  - 新增 `--rerank_margin`
  - 新增 `--rerank_guard`
  - 抽出 `evaluate_reranking(...)` 供批量扫描复用
- [scan_reranking_rules.py](/home/lbh/HiVT/scripts/scan_reranking_rules.py)
  - 单次加载 checkpoint 与 dataloader
  - 批量扫描 `(margin, guard)` 网格
  - 输出汇总 CSV
- [analyze_reranking_case_comparison.py](/home/lbh/HiVT/scripts/analyze_reranking_case_comparison.py)
  - 对比 `margin=0.0` 与 `margin=0.1`
  - 导出逐样本 case 明细与 summary

单测状态：

```text
pytest tests/test_reliability_analysis.py -q

26 passed
```

### 2. rule scan 设置

checkpoint：

```text
/home/lbh/HiVT/runs/hivt_reliability/reliability_20260629_191045_dim64_gpu0/checkpoints/epoch=00-val_minFDE=0.8031.ckpt
```

固定口径：

```text
method       = prob_product
rerank_alpha = 1.0
rerank_top_k = 3
max_batches  = 32
```

扫描网格：

```text
margin = 0.0 / 0.05 / 0.10 / 0.15 / 0.20
guard  = 0.0 / 0.1 / 0.2 / 0.3
```

完整结果见：

- [reranking_rule_scan.csv](/home/lbh/HiVT/docs/reranking_rule_scan.csv)

### 3. rule scan 结果

按 `margin` 聚合后的关键结果如下：

| margin | reranked minMR | top1 change rate | hit->miss | miss->hit |
|---:|---:|---:|---:|---:|
| `0.00` | `0.468750` | `0.250000` | `14` | `3` |
| `0.05` | `0.445312` | `0.121094` | `6` | `1` |
| `0.10` | `0.421875` | `0.023438` | `0` | `1` |
| `0.15` | `0.421875` | `0.011719` | `0` | `1` |
| `0.20` | `0.425781` | `0.000000` | `0` | `0` |

当前可用结论：

1. `margin=0.10` 与 `0.15` 都已达到 `reranked_MR < original_MR`；
2. `margin=0.20` 等价于完全不 rerank，说明再保守一步就会直接抹掉所有收益；
3. 在这一次 mixed-objective checkpoint 扫描中，`margin=0.10` 比 `0.15` 保留更多翻转机会，因此当时更适合作为默认候选；
   但后续独立 `seed=3407` freeze-backbone 复验已经显示，当前主候选应收紧到 `margin=0.15`。
4. `guard` 在当前 checkpoint 上几乎不起作用，说明主矛盾在 `risk gap` 门控，而不是绝对低风险保护。

### 4. `margin=0.0` vs `margin=0.1` case 对比

逐样本明细见：

- [reranking_margin_case_details.csv](/home/lbh/HiVT/docs/reranking_margin_case_details.csv)

summary：

```text
original_MR             = 0.425781
base_MR                 = 0.468750   # margin=0.0
compare_MR              = 0.421875   # margin=0.1

base_switch_count       = 64
compare_switch_count    = 6
blocked_switch_count    = 58

blocked_harmful_count   = 14
blocked_helpful_count   = 2
blocked_neutral_count   = 42
```

risk gap 分位数：

```text
blocked_switch_gap_q10/q50/q90 = 0.016 / 0.044 / 0.083
kept_switch_gap_q10/q50/q90    = 0.119 / 0.153 / 0.195
```

这组分位数非常关键，因为它显示：

1. 被 `margin=0.1` 保留下来的 `6` 次翻转，risk gap 全部已经大于 `0.10`；
2. 被挡住的 `58` 次翻转，即使最高也只有 `0.0965` 左右；
3. 在当前 checkpoint 上，`0.10` 这个门槛几乎把“高把握翻转”和“低把握翻转”清晰切开了。

进一步看 case 类型：

- 被挡住的坏翻转示例：
  - `risk_gap = 0.059`
  - `original_fde = 0.960`
  - `base_fde = 3.097`
- 被挡住的好翻转示例：
  - `risk_gap = 0.015`
  - `original_fde = 2.734`
  - `base_fde = 1.355`
- 唯一保留下来的强收益 `miss -> hit`：
  - `risk_gap = 0.194`
  - `original_fde = 15.908`
  - `reranked_fde = 1.162`

这组 case 给出的解释是：

1. 当前 MR 退化主要不是由“高置信度的大翻转”造成，而是由大量低 gap、低把握翻转累计造成；
2. `margin=0.1` 先挡住了全部 `14` 个 `hit -> miss`，只丢掉 `2` 个弱信号好翻转；
3. 真正强信号的 `miss -> hit` 仍然会被放行；
4. 因此当前第一优先级不是继续放大 near-threshold 监督，而是先把 conservative reranking rule 锁为默认逻辑。

### 5. 当前建议方案（历史结论，已于 2026-07-01 被更新）

基于这轮扫描与 case 复盘，当时的建议曾是直接采用：

```text
prob_product + alpha=1.0 + top_k=3 + margin=0.1 + guard=0.0
```

作为默认 reranking 决策规则。

但这已经被后续两次独立 freeze-backbone 复验更新：

```text
当前正式默认值是
prob_product + alpha=1.0 + top_k=3 + margin=0.15 + guard=0.0
```

原因：

1. 它已经在当前 checkpoint 上实现 `reranked_MR < original_MR`；
2. 它优先消除了对已 hit top-1 的错误替换；
3. 它仍保留了少量高把握的强收益翻转；
4. 它把“rule 问题”和“objective 问题”初步拆开了。

因此，后续所有训练目标复验都应至少同时报告两套口径：

1. 基础复评口径：
   - `prob_product + alpha=1.0 + top_k=3 + margin=0.0 + guard=0.0`
2. 保守决策口径：
   - `prob_product + alpha=1.0 + top_k=3 + margin=0.15 + guard=0.0`

只有在保守决策口径下仍然没有额外收益时，才值得继续把主精力转向更直接的 `top1-vs-best_alt / miss-oriented objective`。

### 6. 后续统一复评口径

从现在开始，所有新 checkpoint 的 reranking 复评都统一遵守下面的记录模板。

必报两套 setting：

1. 基础复评口径
   - `prob_product + alpha=1.0 + top_k=3 + margin=0.0 + guard=0.0`
2. 保守决策口径
   - `prob_product + alpha=1.0 + top_k=3 + margin=0.15 + guard=0.0`

每次复评至少记录：

```text
checkpoint_name
eval_setting_label
original_minADE / original_minFDE / original_minMR
reranked_minADE / reranked_minFDE / reranked_minMR
rerank_top1_change_rate
case_hit_to_miss_count
case_miss_to_hit_count
case_mean_fde_delta
```

当前统一判读顺序：

1. 先看保守决策口径下是否 `reranked_MR <= original_MR`
2. 再看 `hit_to_miss` 是否已经被压到 `0` 或接近 `0`
3. 最后看是否还保留少量高把握 `miss -> hit`

这里“稳定”先按更保守的标准使用：

```text
至少 2 个相互独立的 freeze-backbone checkpoint 上，
同一个固定 margin 值满足：
- reranked_MR <= original_MR
- hit_to_miss = 0
- miss_to_hit >= 1
```

如果只是在“参考 checkpoint + 已混入新 objective 的 checkpoint”上观察到类似现象，
则当前更适合记为“趋势成立”，而不是“rule 已稳定成立”。

如果一个 objective 只在 `margin=0.0` 下显得“有变化”，但在 `margin=0.15` 下没有额外增益，则默认不把它当成当前主线收益来源。

另外，`rerank_guard=0.0` 在当前实现中近似空操作：

- `guard` 只有在非 `None` 时才参与门控
- 当 `guard=0.0` 时，条件近似退化为 `orig_risk > 0`
- 对当前 checkpoint 的风险分布，这通常几乎恒为真

因此当前扫描里 `guard` 几乎不改变结果，更像是现有门控结构的自然结果，而不只是“当前 checkpoint 不需要 guard”。

### 7. 当前执行计划

当前行动顺序已经调整为：

```text
先固化 conservative reranking rule
-> 再做跨 checkpoint 复验
-> 再做中等预算 objective re-eval
-> 最后才决定是否改成 top1-vs-best_alt / miss-oriented objective
```

对应执行文档：

- [2026-07-01-conservative-reranking-next-step-plan.md](/home/lbh/HiVT/docs/superpowers/plans/2026-07-01-conservative-reranking-next-step-plan.md)

### 8. 独立 freeze-backbone checkpoint 复验（seed=3407）

为补上“第二个证据点不独立”的缺口，新增了一次不含新 objective 的独立 freeze-backbone 小预算 run：

```text
run_version          = freeze_backbone_budget128_targetmode067_seed3407
seed                 = 3407
freeze_backbone      = true
rank_loss_weight     = 0.0
threshold_weighting  = false
limit_train_batches  = 128
limit_val_batches    = 32
```

checkpoint：

```text
/home/lbh/HiVT/runs/hivt_reliability/freeze_backbone_budget128_targetmode067_seed3407/checkpoints/epoch=00-val_minFDE=0.8031.ckpt
```

对该 checkpoint 的双口径复评结果：

1. `margin=0.0`

```text
original_minMR             = 0.425781
reranked_minMR             = 0.449219
rerank_top1_change_rate    = 0.218750
case_hit_to_miss_count     = 10
case_miss_to_hit_count     = 4
case_mean_fde_delta        = -0.082791
```

2. `margin=0.1`

```text
original_minMR             = 0.425781
reranked_minMR             = 0.425781
rerank_top1_change_rate    = 0.031250
case_hit_to_miss_count     = 1
case_miss_to_hit_count     = 1
case_mean_fde_delta        = -0.055669
```

继续对该独立 checkpoint 做 `margin scan`，得到：

```text
margin=0.00 -> reranked_MR=0.449219, hit_to_miss=10, miss_to_hit=4
margin=0.05 -> reranked_MR=0.437500, hit_to_miss=4,  miss_to_hit=1
margin=0.10 -> reranked_MR=0.425781, hit_to_miss=1,  miss_to_hit=1
margin=0.15 -> reranked_MR=0.421875, hit_to_miss=0,  miss_to_hit=1
margin=0.20 -> reranked_MR=0.421875, hit_to_miss=0,  miss_to_hit=1
```

同时，`guard=0.0/0.1/0.2/0.3` 在每个 margin 上仍几乎不改变结果。

这轮独立复验带来的更新是：

1. `margin=0.1` 没有在独立 checkpoint 上复现“`hit_to_miss = 0` 且 `reranked_MR < original_MR`”。
2. 因此当前不能再把“`margin=0.1` 已稳定成立”当成默认说法。
3. 但更保守的 `margin=0.15/0.20` 在该独立 checkpoint 上实现了：
   - `reranked_MR = 0.421875 < original_MR = 0.425781`
   - `hit_to_miss = 0`
   - `miss_to_hit = 1`
4. 所以当前更合理的表述应调整为：

```text
“conservative margin family” 作为思路仍成立，
但默认 margin 值尚未锁稳；
当前最强候选更接近 0.15；
0.20 更适合作为收益可能归零的参考上界。
```

更严格地说：

1. `margin=0.15` 是当前唯一在两次扫描里都保持
   `reranked_MR < original_MR`、`hit_to_miss = 0`、`miss_to_hit = 1`
   这一组形态的候选点。
2. `margin=0.20` 在独立 `seed=3407` checkpoint 上仍保留了 `miss_to_hit = 1`，
   但在 mixed-objective checkpoint 上已接近“收益归零”的边界；
   因此它更适合作为 conservative rule 的参考上界，而不是与 `0.15` 同等级并列的默认候选。
3. 随后新增的第二个干净独立 freeze-backbone checkpoint（`seed=9103`）已经补齐了这一步验证：
   `margin=0.15` 在 `seed=3407` 与 `seed=9103` 上都满足
   `reranked_MR < original_MR`、`hit_to_miss = 0`、`miss_to_hit = 1`，
   因此现阶段已达到“至少 2 个独立 freeze-backbone checkpoint”这一稳定性门槛。

### 9. 中等预算 objective re-eval（2026-07-02）

在完成 `rule` 稳定性复验后，按既定计划继续执行第一轮中等预算 mixed-objective re-eval：

```text
run_version                         = freeze_backbone_threshold_objective_budget256_seed9103_v3
seed                                = 9103
freeze_backbone                     = true
scene_target_policy                 = target_best_mode_fail
risk_scene_rate_threshold           = 0.5
rank_loss_weight                    = 0.1
mode_risk_threshold_weight_enabled  = true
mode_risk_threshold_weight_radius   = 0.25
mode_risk_threshold_weight_peak     = 3.0
mode_risk_threshold_weight_base     = 1.0
mode_risk_rank_top_k                = 3
mode_risk_rank_near_threshold_only  = false
mode_risk_rank_threshold_radius     = 0.2
scene_loss_weight                   = 0.05
limit_train_batches                 = 256
limit_val_batches                   = 64
best_checkpoint                     = /home/lbh/HiVT/runs/hivt_reliability/freeze_backbone_threshold_objective_budget256_seed9103_v3/checkpoints/epoch=00-val_minFDE=1.0136.ckpt
```

对应训练监控：

```text
epoch        = 1
global_step  = 256
best_val_minFDE = 1.0136
```

为了避免把 budget 差异和 evaluation window 差异混在一起，下面统一使用同一个 `64 val batches` 口径，对比：

1. reference freeze-backbone checkpoint
2. 中等预算 mixed-objective checkpoint

完整 CSV：

- [reranking_rule_scan_reference_budget128_64batches.csv](/home/lbh/HiVT/docs/reranking_rule_scan_reference_budget128_64batches.csv)
- [reranking_rule_scan_objective_budget256_seed9103_v3.csv](/home/lbh/HiVT/docs/reranking_rule_scan_objective_budget256_seed9103_v3.csv)

关键结果如下：

| checkpoint | margin | original_MR | reranked_MR | hit->miss | miss->hit | top1 change rate |
|---|---:|---:|---:|---:|---:|---:|
| reference freeze-backbone | `0.00` | `0.449219` | `0.470703` | `17` | `6` | `0.203125` |
| reference freeze-backbone | `0.15` | `0.449219` | `0.447266` | `0` | `1` | `0.007812` |
| mixed-objective budget256 | `0.00` | `0.449219` | `0.462891` | `18` | `11` | `0.218750` |
| mixed-objective budget256 | `0.15` | `0.449219` | `0.447266` | `0` | `1` | `0.003906` |

对应的额外观察：

1. 在 `margin=0.0` 下，新 checkpoint 的连续误差与 `miss_to_hit` 数量都比 reference 更活跃，但 `hit_to_miss` 也更高，`reranked_MR` 仍然退化；
2. 在当前默认保守口径 `margin=0.15` 下，两者都得到同一个 `reranked_MR = 0.447266`；
3. 且 `hit_to_miss = 0`、`miss_to_hit = 1` 这一组关键离散 case 统计也没有额外增量；
4. 因此本轮中等预算结果支持的是：

```text
conservative rule 继续有效，
但当前 objective 暂未显示超出 reference baseline 的额外 MR 增量。
```

这条结论的边界也要写清楚：

1. 它足以说明“链路打通，且 rule 与 objective 兼容”；
2. 但它还不够支撑“当前 objective 已被论文级证伪”；
3. 更准确的表述应是：

```text
在单个中等预算 mixed-objective seed 上，
没有观察到在 margin=0.15 之上的额外 MR 收益。
```

这里还要补一个更细的诊断提醒：

1. 在 `64 val batches = 512 cases` 的口径下，`margin=0.15` 本身已经非常保守；
2. reference 与 mixed-objective 两边在该口径下都只保留了极少数真实翻转：
   - reference: `top1_change_rate = 0.007812`，约等于 `4 / 512`
   - mixed-objective: `top1_change_rate = 0.003906`，约等于 `2 / 512`
   - 最后都只留下 `miss_to_hit = 1`
3. 因此当前“无增量”的结论虽然成立，但还存在一个合理替代解释：

```text
margin=0.15 的 conservative rule 可能已经形成 ceiling effect，
把大部分潜在差异过滤掉了。
```

换句话说，现阶段更准确的表述不是：

```text
objective 已经失败
```

而是：

```text
在当前默认保守口径 margin=0.15 下，
尚未观察到额外 MR 增量；
但还不能排除 objective 的细小差异被 conservative rule 掩盖。
```

因此下一步更合适的是：

```text
先补一个 mixed-objective 的第二独立 seed / 复跑；
并把诊断性评估从单点 margin=0.15
扩成 margin = 0.05 / 0.10 / 0.15；
如果在这组 conservative margin 下仍然没有额外 MR 增量或趋势，
再正式转向 top1-vs-best_alt / miss-oriented objective。
```

下一轮判读建议分成两层：

```text
主结论层：
  margin=0.15 下是否出现稳定额外 MR 增量

诊断层：
  margin=0.05 / 0.10 下是否出现更高的 miss_to_hit 趋势，
  用来区分“objective 真无效”与“rule ceiling 掩盖差异”
```
