# 实验问题阶段性解决方案文档

## 1. 文档目的

本文档用于记录 `HiVT + reliability plugin` 实验中已经暴露并修复的标签塌缩问题、对应修复方案，以及当前仍需解决的实验问题。

截至 2026-06-27，reliability supervision 的标签塌缩问题已经通过 conservative fix 得到解决。当前重点已经从“修标签”转为验证 reliability 分支如何在不破坏宿主预测性能的前提下发挥作用，优先方向是 `freeze backbone` 与 `reranking-only` 对照。

本阶段的核心原则是：

```text
先让 risk label 可学，再让 risk score 有用，最后再让模块更完整、更像论文。
```

## 2. 当前实验状态

当前插件已经完成了结构和训练链路的最小闭环：

- `mode_risk` / `scene_risk` / `reranked_pi` 已接入 HiVT。
- FDE / ADE / miss / conflict / off-road 自动标签已接入。
- `L_pred + L_mode + L_scene` 联合训练已跑通。
- `AUROC` / `BrierScore` / `ECE` 等可靠性指标已接入验证流程。
- 小预算 baseline/plugin 对照实验已经完成。

阶段性实验结果表明：

- 标签塌缩已解除，`mode / scene / conflict` 标签恢复到可解释区间。
- reliability head 已表现出可判别的失败风险估计能力。
- 当前联合训练设置会轻微拉低宿主原始预测指标。
- 插件尚未证明能稳定提升预测精度，下一步应优先验证冻结主干或只做 reranking 的方案。

## 3. 已修复问题回顾

### 3.1 Risk 标签塌缩（已修复）

修复前实验中出现过如下现象：

- `train_mode_risk_target_rate = 1.0`
- `val_mode_risk_target_rate = 1.0`
- `train_conflict_risk_target_rate = 1.0`
- `val_conflict_risk_target_rate = 1.0`
- `train_scene_risk_target_rate = 1.0`
- `val_scene_risk_target_rate = 1.0`
- `mode_risk_pred_mean` 和 `scene_risk_pred_mean` 接近 `1.0`

这说明当时的风险监督没有正负样本区分度。模型最优策略变成直接预测所有 mode 和所有 scene 都是高风险。

该问题已通过 `fde_only + target_mode_rate(rho=0.67) + target_to_neighbors conflict` 修复。修复后 `val_mode_risk_target_rate=0.4591`，`val_scene_risk_target_rate=0.3945`，`val_conflict_risk_target_rate=0.0007`。

### 3.2 Conflict 标签过密（已修复）

修复前 conflict 标签容易把密集场景全部判为冲突，主要原因包括：

- 使用轨迹之间的最小距离作为代理，定义过宽。
- 缺少严格的同一时间步碰撞判断。
- 缺少持续帧数约束。
- 可能包含过多无关 actor 之间的两两比较。
- 没有充分利用有效 actor / 有效 future mask。

该问题已修复：当前 `conflict_targets` 使用 target actor 预测轨迹与邻车 GT future 的同时间步持续接近代理，且默认不进入主 `mode_targets`。

### 3.3 Off-road 标签仍是弱代理

当前 off-road 标签主要依赖 lane centerline distance，而不是 drivable-area polygon 或 raster。

这个代理存在两个问题：

- 可能把合法变道、路口区域、lane-adjacent 轨迹误判为 off-road。
- 也可能漏掉真实驶出可行驶区域的轨迹。

因此当前 off-road 不适合作为主风险监督，只适合作为辅助日志或弱安全代理。

### 3.4 Scene Risk 聚合过激（已修复）

修复前 scene risk 采用类似 `max over node/mode risk` 的聚合方式。

在多 actor、多 mode 场景中，只要任意 actor 的任意 mode 被判为高风险，整个 scene 就会被标成高风险。这会导致 `scene_risk_target_rate` 很容易接近 `1.0`。该问题已通过 `scene_target_policy=target_mode_rate` 与 `risk_scene_rate_threshold=0.67` 修复。

### 3.5 主风险标签被子标签污染

修复前主风险标签近似为：

```text
mode_targets = max(fde, ade, miss, conflict, offroad)
```

这种并集式定义在早期阶段风险很高。当前默认已改为 `mode_target_policy=fde_only`，五类并集只作为 `all_union` 对照保留。

## 4. 当前阶段目标

标签修复目标已经完成。当前阶段的目标不是继续修标签，而是验证 reliability 分支如何在不破坏宿主 HiVT 精度的前提下提供可判别风险估计。

已完成的第一阶段验收结果：

- `mode_risk_target_rate` 已不再接近 `1.0` 或 `0.0`。
- `scene_risk_target_rate` 已不再接近 `1.0` 或 `0.0`。
- `conflict_risk_target_rate` 已降至接近 0 的稀疏安全事件区间。
- `mode_risk_pred_mean` 已不再逼近 `1.0`。
- `AUROC` / `AUPRC` / `BrierScore` / `ECE` 已具备基本解释意义。

经验上可以先参考下面的分布范围：

- `mode_risk_target_rate`: 约 `0.15 ~ 0.45`
- `scene_risk_target_rate`: 约 `0.2 ~ 0.6`

这些范围不是硬性目标。标签必须先有物理意义，再检查分布是否适合训练。

## 5. 已执行修复路线与后续路线

当前不再按“先修标签”的阶段推进，因为保守标签修复已经完成。后续路线调整如下。

### 5.1 保守修复版（已完成）

本轮已经在不改 HiVT backbone 的前提下，完成标签定义和聚合策略修复。

已完成目标：

- 验证塌缩主要来自标签定义。
- 恢复 mode-level 和 scene-level 风险监督的可判别性。
- 保持当前训练链路稳定。

该路线已经通过 `64/16` 与 `128/32` 小预算实验验证。

### 5.2 冻结主干 / Reranking-only 验证版（当前优先）

当前最优先的是避免 reliability loss 干扰已训练好的预测主干，验证风险分数本身是否有部署价值：

- 冻结 HiVT backbone，只训练 `reliability_module`。
- 或保持当前 checkpoint，仅比较 `original pi` 与 `reranked_pi`。
- 暂不提高 `rank_loss_weight` / `calib_loss_weight`。

目标是验证风险分数是否能改善 mode selection，且不损伤 baseline 的 `minADE / minFDE / MR`。

### 5.3 多任务论文版

在 freeze backbone / reranking-only 验证通过后，再把风险源拆成多个任务：

- prediction failure risk
- conflict risk
- off-road risk
- scene risk

目标是增强可解释性和论文完整度。但这一版实现和调参更复杂，应放在 freeze backbone / reranking-only 验证之后推进。

## 6. 已采用标签配置

### 6.1 Mode Risk 主监督

当前已经不再使用五类标签并集作为默认主监督。

已采用的主 `mode_targets` 是预测失败标签：

```text
y_mode = 1(FDE_k > tau_fail)
```

保留但暂不作为默认的备选策略：

```text
y_mode = 1(FDE_k > tau_fail) OR 1(MR_k = 1)
```

其中 `MR_k` 应对应明确的 miss threshold。如果 `tau_fail` 与 `miss_threshold` 都基于 FDE，需要避免两个阈值在 OR 之后退化成单一较小阈值。当前实现中：

- `miss_targets`: 与 MR 指标一致，表示是否超过 miss threshold。
- `fde_targets`: 更宽或更严格的预测失败阈值。
- `mode_targets`: 本阶段优先选一个清晰定义，而不是盲目 OR 多个阈值。
- `risk_fde_threshold` 与 `risk_miss_threshold` 必须分工明确，不能只是在代码里重复定义两次终点误差阈值。

当前采用的阈值关系：

```text
risk_fde_threshold = 2.0
risk_miss_threshold = 4.0
```

含义建议固定为：

- `risk_fde_threshold`: 用于定义“预测失败但不一定彻底 miss”的 mode-level failure。
- `risk_miss_threshold`: 用于定义“明显 miss GT”的更强失败事件。

如果 `risk_miss_threshold > risk_fde_threshold`，则：

```text
fde_or_miss = fde_only
```

在逻辑上几乎退化。因此当前默认：

- 主监督默认直接使用 `fde_only`。
- `miss_targets` 先保留为辅助统计和日志。
- `fde_or_miss` 只作为显式对照策略保留。

短期内，`ADE` 不进入主风险标签，只作为辅助日志保留。原因是 ADE 与 FDE 混合后会让失败定义变得不够清晰。

### 6.2 Conflict 标签

Conflict 已经从宽松的轨迹最小距离代理，改成更严格的时序碰撞代理。

当前定义：

```text
conflict = same_time_distance(target_mode_k(t), neighbor_future_gt(t)) < tau_col
           AND close_frames >= m
           AND target_future_valid(t)
           AND neighbor_future_valid(t)
```

当前参数：

```text
tau_col = 1.0m
m = 2 frames
```

当前采用：

- `target actor` 的第 `k` 条预测 future
- 对比同 scene 下每个有效邻车的 `GT future`
- 仅做同一时间步距离比较
- 对每个邻车只在双方 future mask 同时有效的步上计算
- 若任一邻车满足持续接近条件，则该 `target mode k` 记为 conflict

当前比较范围：

- 只比较 target actor 的预测轨迹与有效邻车 future。
- 暂不做全场景所有 actor 两两 mode 比较。
- 使用 actor valid mask、target future mask、neighbor future mask。
- 可过滤距离过远或动态状态无效的邻车。

不建议第一阶段使用下面这种定义作为主实现：

```text
target prediction vs neighbor prediction
all modes vs all modes
all actors vs all actors
```

这些定义更适合作为后续诊断或辅助统计，因为它们会显著抬高密集交通场景里的 `conflict_risk_target_rate`。

短期内，`conflict` 不进入主 `mode_targets`，只作为：

- `conflict_risk_target_rate`
- 安全代理指标
- 后续辅助 head 标签

这样可以避免 conflict 再次污染主风险监督。

### 6.3 Off-road 标签

当前 off-road 标签不建议作为主监督。

短期策略：

- 从主 `mode_targets` 中移除。
- 保留 `offroad_risk_target_rate` 日志。
- 在文档或代码注释中明确它是 lane support proxy，而不是真正 drivable-area violation。

中期优化：

- 使用 drivable-area polygon 或 raster 判断。
- 引入 lane segment 宽度，而不是只看 centerline distance。
- 要求连续若干 future step 越界，避免单点噪声触发。

### 6.4 Scene Risk 聚合

当前已经不再默认使用全场景 hard max：

```text
y_scene = max over all nodes/modes y_mode
```

当前采用 target-centric 的比例聚合，避免“target actor 只要任意 mode 为正，整个 scene 就为正”这种过密定义。

已验证的策略如下：

```text
历史候选 A:
y_scene = 1(target actor best/min-FDE mode failed)
```

```text
当前采用 B:
y_scene = 1(target actor positive mode rate > rho)
```

不推荐第一阶段默认使用：

```text
y_scene = 1(target actor has any positive mode)
```

因为在多模态设置下，只要目标车有一部分 mode 明显偏离 GT，这个定义就很容易再次把 `scene_risk_target_rate` 推高到接近 `1.0`。

更具体地说，推荐 scene policy 语义改成：

```text
target_best_mode_fail
```

或：

```text
target_mode_rate
```

如果需要保留更强的 scene-level 概念，可以后续使用比例聚合：

```text
y_scene = 1(high_risk_mode_rate > rho)
```

当前采用 `target_mode_rate`，`rho=0.67`。它在宿主 checkpoint 审计中得到 `scene_positive_rate=0.4063`，比 `target_best_mode_fail` 更适合当前训练闭环。

### 6.5 Target Actor 索引与标签归属

本轮修复中，所有 target-centric 标签策略都不能只停留在文档层面，必须在实现上显式传入目标 actor 索引。

建议在 `build_reliability_targets` 及相关子函数中新增：

```text
agent_index
```

并约定：

- `agent_index` 来自每个 graph 的目标车辆索引。
- `target_to_neighbors` conflict 策略依赖 `agent_index` 才能正确找到 target actor。
- `target_best_mode_fail` / `target_mode_rate` 这类 scene policy 也依赖 `agent_index`。
- 若缺少 `agent_index`，则 target-centric 策略应直接报错或显式回退，不应静默退化成全 actor 聚合。

## 7. 已完成代码修改落点

### 7.1 `models/reliability.py`

已修改以下函数：

- `compute_mode_risk_targets`
- `compute_miss_risk_targets`
- `compute_conflict_risk_targets`
- `compute_offroad_risk_targets`
- `compute_scene_risk_targets`
- `build_reliability_targets`
- `summarize_reliability_targets`

已新增或调整的能力：

- 支持 `mode_target_policy`，例如 `fde_only`、`miss_only`、`fde_or_miss`、`all_union`。
- 支持 `scene_target_policy`，例如 `target_best_mode_fail`、`target_mode_rate`、`scene_rate`、`scene_max`。
- 支持 `conflict_min_frames`。
- 支持 `conflict_scope`，例如 `target_to_neighbors`、`all_valid_pairs`。
- `build_reliability_targets` 显式接收 `agent_index`，而不是在 target-centric 策略中隐式假设第 0 个 actor 就是 target actor。
- `compute_conflict_risk_targets` 明确支持“target prediction vs neighbor GT future”的第一阶段实现。
- 在 summary 中单独记录每个子标签，不只记录最终并集标签。

### 7.2 `models/hivt.py`

训练接入处需要做三类调整：

- 将新的 label policy 参数传入 `build_reliability_targets`。
- 将 `data['agent_index']` 传入 `build_reliability_targets`，不要只在验证指标里使用。
- 训练日志中继续输出 `{mode/fde/miss/conflict/offroad/scene}_risk_target_rate`。
- 补齐 `train_ade_risk_target_rate`、`train_miss_risk_target_rate`、`val_ade_risk_target_rate`、`val_miss_risk_target_rate`。
- 当前默认 `rerank_alpha=0`，先把 reranking 作为单独对照实验处理。

### 7.3 `train.py` 与 `run_single_gpu.sh`

建议增加 CLI 参数或环境变量：

```text
--mode_target_policy
--scene_target_policy
--risk_fde_threshold
--risk_miss_threshold
--risk_conflict_threshold
--risk_conflict_min_frames
--risk_conflict_scope
--risk_scene_rate_threshold
```

这些参数用于快速跑标签审计和小预算复验，不需要反复改代码。

### 7.4 新增标签审计脚本

建议新增脚本：

```text
scripts/audit_reliability_targets.py
```

脚本功能：

- 加载 train 或 val dataloader。
- 跑若干 batch forward。
- 不反向传播。
- 统计不同阈值和策略下的正样本率。
- 输出表格，帮助选择 `tau_fail`、`tau_col`、`rho`。

至少输出：

```text
fde_risk_target_rate
miss_risk_target_rate
conflict_risk_target_rate
offroad_risk_target_rate
mode_risk_target_rate
scene_risk_target_rate
mode_risk_pred_mean
scene_risk_pred_mean
```

建议额外输出：

```text
target_actor_mode_risk_target_rate
non_target_actor_mode_risk_target_rate
target_best_mode_fail_rate
scene_count / actor_count / valid_actor_count
min_pair_dist 或 close_frames 的分位数统计
```

这样才能区分“主标签真的恢复可分”与“只是被邻车标签污染得不那么极端”。

## 8. 当前训练策略

### 8.1 当前已采用配置

当前推荐配置已经固定为：

```text
mode_target_policy = fde_only
scene_target_policy = target_mode_rate
risk_scene_rate_threshold = 0.67
risk_fde_threshold = 2.0
risk_miss_threshold = 4.0
risk_conflict_threshold = 1.0
risk_conflict_min_frames = 2
risk_conflict_scope = target_to_neighbors
reliability_loss_weight = 1.0
scene_loss_weight = 0.2
rank_loss_weight = 0
calib_loss_weight = 0
rerank_alpha = 0
```

该配置已经通过 `64/16` 与 `128/32` 小预算实验验证，标签分布不再塌缩，risk head 已具备可判别性。

### 8.2 后续可打开项

后续不应直接把所有复杂项一起打开。推荐顺序是：

```text
1. freeze backbone，只训练 reliability_module
2. reranking-only，对比 original pi 与 reranked_pi
3. 小权重打开 rank_loss_weight = 0.05 ~ 0.1
4. calib_loss_weight 继续从 0.0 起步
5. rerank_alpha 从 0.2 逐步试到 0.5
```

`scene_loss_weight` 仍不宜过大，因为 scene label 比 mode label 更粗，权重过大可能干扰 mode-level 学习。

### 8.3 当前指标解释顺序

当前标签已修复，指标解释顺序调整为：

1. 插件是否不破坏宿主 `minADE / minFDE / MR`。
2. `original pi` 与 `reranked_pi` 的 mode selection 是否有差异。
3. `AUROC / AUPRC / BrierScore / ECE` 是否维持可解释。
4. risk score 是否对高 FDE / miss mode 有更高响应。
5. shift 条件下 risk score 是否随 failure rate 上升。

## 9. 实验闭环设计

### 9.1 已完成实验

已完成：

- 标签体检：基于宿主 `HiVT-64` checkpoint 完成。
- `64/16` 小预算训练验证：标签分布和 risk head 均恢复可解释。
- `128/32` 小预算对照：可靠性指标稳定，但联合训练轻微损伤宿主预测指标。

因此当前不再继续做“标签是否塌缩”的验证。

### 9.2 下一步：Reranking 单独验证

使用同一个 checkpoint 分别评估：

```text
original pi
reranked_pi
```

关注：

- `minFDE`
- `MR`
- `minADE`
- top-1 FDE
- failure detection AUROC

如果 reranking 后 `MR` 或 `minFDE` 改善，同时 `minADE` 不明显恶化，可以支持 risk-aware reranking 的价值。

### 9.3 下一步：Freeze Backbone 验证

冻结 HiVT backbone，只训练 `reliability_module`：

- backbone / decoder 参数不更新。
- `reliability_loss_weight=1.0`
- `scene_loss_weight=0.2`
- `rank_loss_weight=0`
- `calib_loss_weight=0`

目标是确认 reliability 分支能学习风险，而不拉低宿主原始预测指标。

### 9.4 后续：Shift Evaluation

在 risk head 有判别性之后，再打开：

- `HistoryDropout`
- `NeighborDropout`
- `StateNoise`
- `MapJitter`

重点验证：

```text
shift 强度上升 -> failure rate 上升 -> risk score 同步上升
```

同时比较 risk score 与原始 `pi` 在失败检测上的差异。

## 10. 验收标准

已经完成的标签修复标准：

- `mode_risk_target_rate` 不再等于 `1.0`。
- `scene_risk_target_rate` 不再等于 `1.0`。
- `conflict_risk_target_rate` 不再等于 `1.0`。
- `mode_risk_pred_mean` 不再接近 `1.0`。
- `AUROC` / `AUPRC` 能正常反映风险区分能力。
- 训练 loss 不出现异常震荡或 NaN。

当前进一步成功标准：

- freeze backbone 后，宿主 `minADE / minFDE / MR` 不显著劣化。
- reranking 后 `MR` 或 `minFDE` 有改善。
- risk score 对高 FDE / miss mode 有明显更高响应。
- shift 条件下 risk score 随失败率上升。

## 11. 推荐实施顺序

### 11.1 第一轮：Freeze Backbone

1. 从宿主 `HiVT-64` checkpoint 初始化。
2. 冻结 backbone / decoder。
3. 只训练 `reliability_module`。
4. 使用 `128/32` 或略大的预算复验。
5. 对照 baseline checkpoint，确认预测指标不显著劣化。

### 11.2 第二轮：Reranking-only

1. 使用同一个 checkpoint 对比 `original pi` 和 `reranked_pi`。
2. 扫描 `rerank_alpha = 0.2 / 0.5 / 1.0`。
3. 重点看 `minFDE / MR / minADE`。
4. 若 reranking 无改善，先检查 risk score 与 FDE 的相关性。

### 11.3 第三轮：恢复论文级实验

1. 加入 shift evaluation。
2. 重新跑 baseline/plugin 对照。
3. 做必要消融。
4. 最后再考虑多 backbone 泛化。

## 12. 论文表述调整

当前阶段不应表述为：

```text
插件显著提升 HiVT 预测精度
```

更稳妥的表述是：

```text
在基本不破坏宿主预测性能的前提下，引入可判别的 mode-level 与 scene-level 失败风险估计，并通过 risk-aware reranking 和 shift-aware evaluation 体现部署价值。
```

论文创新点应聚焦于：

- 自动风险标签协议。
- mode-level + scene-level 双层可靠性。
- interaction-aware conflict proxy。
- shift-aware reliability evaluation。
- risk-aware reranking。
- plug-in 形式接入宿主轨迹预测模型。

## 13. 当前不建议做的事情

在确认 freeze backbone / reranking-only 不破坏宿主预测指标之前，不建议：

- 直接跑长时间正式训练。
- 直接做 QCNet / MTR / Wayformer 泛化。
- 直接加复杂多 head 并提高损失权重。
- 宣称插件已经提升预测精度。

这些工作应在确认可靠性分支不会拉低宿主预测指标之后再推进。

## 14. 简短结论

当前 idea 的结构闭环已经打通，自动风险标签协议的塌缩问题也已经修复。当前主要问题是：联合训练会轻微牺牲宿主原始预测指标。

下一步最优先的工作是做 `freeze backbone` 或 `reranking-only` 对照，确认 reliability 分支能在不破坏宿主预测指标的前提下提供部署价值。

## 15. 阶段性修复实验记录（2026-06-27）

### 15.1 代码修复内容

本轮已经完成第一阶段 conservative fix 的主要代码闭环：

- 主 `mode_targets` 默认改为 `fde_only`，不再默认并入 `ADE / miss / conflict / offroad`。
- `conflict_targets` 改为 `target prediction vs neighbor GT future` 的同时间步持续接近代理。
- `scene_targets` 支持 `target_best_mode_fail`、`target_mode_rate`、`scene_rate`、`scene_max`。
- 训练与验证都显式传入 `agent_index`，避免 target-centric 策略退化为全 actor 聚合。
- 新增 `scripts/audit_reliability_targets.py`，支持加载宿主 checkpoint 做无反传标签体检。
- 训练日志补齐 `ADE / miss / AUPRC` 等指标。

验证已通过：

```text
pytest tests/test_audit_reliability_targets.py tests/test_reliability.py tests/test_reliability_presets.py -q
13 passed
```

### 15.2 Label Audit 结果

注意：第一次直接用随机初始化模型做 audit 时，`target_best_mode_fail` 会因为预测轨迹本身很差而严重偏正，因此不能作为阈值选择依据。后续 audit 改为加载已训练宿主模型：

```text
/home/lbh/HiVT/checkpoints/HiVT-64/checkpoints/epoch=63-step=411903.ckpt
```

在宿主 `HiVT-64` checkpoint 上，默认 `target_best_mode_fail` 的结果为：

```text
mode_positive_rate          = 0.4085
fde_positive_rate           = 0.4085
miss_positive_rate          = 0.2156
conflict_positive_rate      = 0.0008
offroad_positive_rate       = 0.1815
scene_positive_rate         = 0.0625
target_actor_mode_rate      = 0.6823
target_best_mode_fail_rate  = 0.0625
```

结论：

- 主 `mode` 标签已经恢复到可训练区间。
- `conflict` 已降至稀疏安全事件区间，且不再污染主监督。
- `target_best_mode_fail` 作为 scene label 过于保守，`scene_positive_rate` 偏低。

随后扫描 `scene_target_policy=target_mode_rate`：

| scene policy | rho | scene_positive_rate | 结论 |
|---|---:|---:|---|
| `target_mode_rate` | 0.50 | 0.7500 | 偏高 |
| `target_mode_rate` | 0.67 | 0.4063 | 合适 |
| `target_mode_rate` | 0.75 | 0.4063 | 合适 |

因此当前推荐的阶段性配置为：

```text
mode_target_policy=fde_only
scene_target_policy=target_mode_rate
risk_scene_rate_threshold=0.67
risk_fde_threshold=2.0
risk_miss_threshold=4.0
risk_conflict_threshold=1.0
risk_conflict_min_frames=2
risk_conflict_scope=target_to_neighbors
reliability_rerank_alpha=0.0
scene_loss_weight=0.2
rank_loss_weight=0.0
calib_loss_weight=0.0
```

### 15.3 小预算训练验证

实验设置：

- 初始化：宿主 `HiVT-64` checkpoint，`reliability_module` 随机初始化。
- 训练预算：`max_epochs=1`，`limit_train_batches=64`，`limit_val_batches=16`。
- batch size：`train_batch_size=8`，`val_batch_size=8`。
- 配置：`fde_only + target_mode_rate(rho=0.67)`。
- 日志目录：`/home/lbh/HiVT/runs/hivt_reliability/stage1_fix_budget64_fdeonly_targetmode067`。

关键结果：

```text
train_mode_risk_target_rate    = 0.4386
train_scene_risk_target_rate   = 0.4805
train_conflict_risk_target_rate= 0.0007
train_mode_risk_pred_mean      = 0.4569
train_scene_risk_pred_mean     = 0.4815

val_mode_risk_target_rate      = 0.4571
val_scene_risk_target_rate     = 0.3359
val_conflict_risk_target_rate  = 0.0007
val_mode_risk_pred_mean        = 0.5314
val_scene_risk_pred_mean       = 0.4160

val_mode_AUROC                 = 0.8522
val_mode_AUPRC                 = 0.7964
val_mode_BrierScore            = 0.1592
val_mode_ECE                   = 0.0691

val_minADE                     = 0.6223
val_minFDE                     = 0.9509
val_minMR                      = 0.0703
```

阶段性结论：

1. 标签塌缩已被解除。`mode / scene / conflict` 三类标签都处于可解释区间。
2. `mode` 风险监督已经具备可判别性，`AUROC=0.8522`、`AUPRC=0.7964`。
3. risk prediction 没有继续塌缩到全 1，`mode_risk_pred_mean` 与 `scene_risk_pred_mean` 均处在可解释区间。
4. 宿主预测指标没有被明显破坏；当前阶段更应强调“可靠性监督恢复有效”，而不是宣称预测精度已显著提升。

下一步：

- 使用同一推荐配置跑更完整的小预算对照。
- 对照 baseline checkpoint 的原始验证指标，确认插件训练后没有破坏宿主性能。
- 之后再进入 reranking 前后对照与 shift 条件验证。

### 15.4 更完整小预算对照

为确认 `64/16` 结果不是偶然，本轮继续使用同一推荐配置跑更完整的小预算对照。

实验设置：

- 宿主 checkpoint：`/home/lbh/HiVT/checkpoints/HiVT-64/checkpoints/epoch=63-step=411903.ckpt`
- Baseline：直接验证宿主 `HiVT-64` checkpoint。
- Plugin：宿主 checkpoint 初始化 backbone，`reliability_module` 随机初始化。
- Plugin 训练预算：`max_epochs=1`，`limit_train_batches=128`，`limit_val_batches=32`。
- Baseline 验证预算：`limit_val_batches=32`。
- batch size：`train_batch_size=8`，`val_batch_size=8`。
- Plugin 配置：`fde_only + target_mode_rate(rho=0.67)`。
- Plugin 日志目录：`/home/lbh/HiVT/runs/hivt_reliability/stage1_fix_budget128_fdeonly_targetmode067`。

Baseline 结果：

```text
val_reg_loss = -0.3638
val_minADE   = 0.5738
val_minFDE   = 0.8031
val_minMR    = 0.0508
```

Plugin 结果：

```text
val_reg_loss                 = -0.2850
val_minADE                   = 0.6110
val_minFDE                   = 0.8664
val_minMR                    = 0.0703

val_mode_risk_target_rate    = 0.4591
val_scene_risk_target_rate   = 0.3945
val_conflict_risk_target_rate= 0.0007
val_mode_risk_pred_mean      = 0.4738
val_scene_risk_pred_mean     = 0.6350

val_mode_AUROC               = 0.8735
val_mode_AUPRC               = 0.8358
val_mode_BrierScore          = 0.1445
val_mode_ECE                 = 0.0302
```

对照结论：

1. 标签修复效果在更大的小预算设置下仍然稳定：
   - `val_mode_risk_target_rate=0.4591`
   - `val_scene_risk_target_rate=0.3945`
   - `val_conflict_risk_target_rate=0.0007`
2. 可靠性判别能力进一步增强：
   - `AUROC: 0.8522 -> 0.8735`
   - `AUPRC: 0.7964 -> 0.8358`
   - `ECE: 0.0691 -> 0.0302`
3. 当前 plugin 训练会轻微牺牲宿主原始预测指标：
   - `minADE: 0.5738 -> 0.6110`
   - `minFDE: 0.8031 -> 0.8664`
   - `MR: 0.0508 -> 0.0703`
4. 因此当前阶段的正确结论是：

```text
标签协议修复已经成功，可靠性监督恢复可判别性；但在当前联合训练设置下，插件尚未证明能提升宿主预测精度，下一步应优先做 reranking-only 或冻结 backbone 的对照，避免 reliability loss 干扰已经训练好的预测主干。
```

推荐下一步：

- 冻结 HiVT backbone，只训练 `reliability_module`，确认是否能在不破坏预测指标的情况下学习风险。
- 或者保持当前 checkpoint，仅评估 `original pi` vs `reranked_pi`，单独验证 risk-aware reranking 是否改善 `minFDE / MR`。
- 暂时不要扩大到长训练或多 backbone，直到确认 reliability 分支不会拉低宿主预测指标。
